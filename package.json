{
  "name": "techara",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"npm -C server run dev\" \"npm -C client run dev\"",
    "build": "npm -C client run build && npm -C server run build",
    "start": "npm -C server run start",
    "check": "npm -C client run check && npm -C server run check",
    "db:push": "npm -C server run db:push"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "drizzle-orm": "^0.45.1",
    "drizzle-zod": "^0.8.3",
    "zod": "^4.2.1"
  }
}
