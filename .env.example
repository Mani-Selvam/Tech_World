# === REPLIT ENVIRONMENT (Auto-populated) ===
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
PGHOST=localhost
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=dbname

# === LOCAL DEVELOPMENT ===
NODE_ENV=development
PORT=8000
VITE_API_URL=http://localhost:8000

# === PRODUCTION DEPLOYMENT ===
# Uncomment and update these for production
# NODE_ENV=production
# PORT=5000
# VITE_API_URL=https://api.yourdomain.com

# === CPANEL DEPLOYMENT ===
# For cPanel deployment, use your actual domain:
# VITE_API_URL=https://yourusername.cpanel.yourdomain.com:port
# or if on same server:
# VITE_API_URL=https://yourdomain.com/api
